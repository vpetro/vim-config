
# TODO


# November 15th, 2025

* add inline hints
    * turns out that they have to be enabled with a key binding before they show up
    * dont expect them to be enabled and showing by default
* add blink-cmp for completion
    * add is easy, just install the plugin and call setup
    * turned off auto-showing completion
    * turned off auto-selecting values
    * turned on signature help
* add python lsp with basedpyright
* configure treesitter
    * enable support for lua, python and others
    * make sure that the objects work
* better configuration for fzf-lua
    * push the window to the bottom
    * define old fzf.vim commads to use fzf-lua
* setup go lsp
* fix loading of none-ls
    * it is not a real lsp server, so it has to be loaded as a plugin
* add isort for formatting python imports
* setup tv (from astral) to use for type checking
